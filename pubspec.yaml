name: rtmp_broadcaster_mux
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.5
  permission_handler: ^11.3.1
  rtmp_broadcaster: ^2.2.6
  video_player: ^2.9.2
  wakelock: ^0.6.2
  win32: ^2.5.0

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
